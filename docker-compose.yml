services:
  lms-app:
    build: .
    container_name: lms-app
    env_file:
      - .env
    depends_on:
      lms-db:
        condition: service_healthy
    volumes:
      - .:/app
      - /app/node_modules
    command: sh -c "npx prisma migrate deploy && npm run build && npm run start:dev"
    restart: always
    networks:
      - nginx-proxy
      - default

  lms-db:
    image: mysql:8.0
    container_name: lms-db
    env_file:
      - .env
    ports:
      - '3306:3306'
    healthcheck:
      test: ['CMD', 'mysqladmin', 'ping', '-h', 'localhost']
      interval: 5s
      retries: 5
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - default

networks:
  nginx-proxy:
    external: true

volumes:
  db_data:
